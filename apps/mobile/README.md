# @quran/mobile

Placeholder for the mobile client (React Native + Expo). Not yet scaffolded.

When initializing, use Expo with TypeScript and wire into the monorepo as `@quran/mobile`, consuming `@quran/core`, `@quran/data`, `@quran/i18n`.
